# 貢献の方法

本リポジトリへの提案や修正を歓迎します。Issue や Pull Request は日本語・英語のどちらでもかまいません。

## Pull Request を送る前に

次のコマンドがすべて成功することをご確認ください。

```bash
make test   # Go と Python のテスト
make lint   # Python の静的検査（ruff）
make model  # パイプライン全体（ネットワーク・データ不要）
```

CI では macOS 上で同じ内容を実行し、あわせて gitleaks による秘密情報の検査も行います。

## お受けできないもの

**レースのデータ。** オッズ、結果、払戻、取得したウェブページなどは、形式や量にかかわらずお受けできません。これらには提供元の権利があるためです。テストにデータが必要な場合は、`pykeiba.synth`（合成データの生成器）を拡張してください。

**時系列での検証を経ていない「利益が出る」という主張。** 確定した結果や確定オッズ、評価期間より後のレースを参照して改善したバックテストは、有効な結果とはいえません。データを扱う際の決まりごとは [docs/04-data-contract.md](docs/04-data-contract.md) にまとめています。

**環境変数以外の場所に書かれた認証情報。** 設定ファイル、launchd の設定、テスト用のデータなどに認証情報を含めないでください。

## とくに歓迎するもの

- ご自身が取得する権利を持つデータ源から、本リポジトリの形式でデータを出力する収集プログラム（サーバに負担をかけない設定を既定にしたもの）
- 方策表の価格帯ごとの払戻率について、より良い推定方法（同梱の値は公表されている控除率をもとにした目安です）
- macOS 以外の環境への対応（既存の動作を変えず、`scripts/` への追加として）

## コードの書き方

- Go: `gofmt` で整形し、できるだけ標準ライブラリを使ってください。
- Python: `ruff` の設定（1行120文字）に従い、公開する関数には型注釈を付けてください。

コメントには「なぜそうしているのか」を書いてください。とくに、あえて慎重な処理にしている箇所（`pykeiba/dp.py` の所持ポイントの切り捨て、`internal/voting` の再開時に読み返しだけを行う処理など）を変更する場合は、同じコミットでコメントも更新してください。

ソースコード中のコメントは英語で統一しています。日本語での説明は `docs/` に追加してください。

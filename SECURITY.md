# セキュリティ

## 報告の方法

脆弱性を見つけられた場合は、公開の Issue ではなく、GitHub の非公開の報告機能（リポジトリの「Security」タブ →「Report a vulnerability」）からご連絡ください。とくに、認証情報や送信内容の正しさに関わるものは、公開の場に書かないようお願いします。

## 対象とするもの

- 運用者が許可していない送信、または許可した内容と異なる送信が起こりうる問題
- `KEIBA_LOGIN_ID` や `KEIBA_PASSWORD` が、ログ、状態ファイル、ジャーナル、launchd の設定ファイルなどに漏れる問題
- 運用者が確認したハッシュ値と異なる計画ファイルが、検証を通ってしまう問題

## 対象外のもの

- 大会の投票 API そのものの問題。本リポジトリの管理外のため、提供元へご連絡ください。
- 仮想ポイントが減ること。この手法は平均すると損をするものです（[DISCLAIMER.md](DISCLAIMER.md) 参照）。

## 設計についての補足

送信には独立した5つの条件がそろう必要があります（[docs/06-safety.md](docs/06-safety.md) 参照）。既定では模擬投票ドライバが使われ、ネットワークには接続しません。認証情報は環境変数からのみ読み込み、選択したドライバが本番の場合にだけ使います。
